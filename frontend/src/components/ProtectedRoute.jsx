// TODO: implement ProtectedRoute component
// - Si loading: mostrar spinner
// - Si no hay token: redirigir a /login
// - Si user.status === 'pending': mostrar pantalla "Pendiente de aprobación"
// - Si user.status === 'inactive': mostrar pantalla "Cuenta desactivada"
// - Si user.status === 'active': renderizar children
export default function ProtectedRoute({ children }) {
  return children
}
